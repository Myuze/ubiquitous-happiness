{{log 'this' games}}
{{#each games as |game|}}
<section class="forums">
      <a href="/home/{{id}}">
          <div class="game-header">
            <h4>{{game_name}}</h1>
          </div>
      </a>
</section>
<img src="{{game_image_URL}}">
{{/each}}


